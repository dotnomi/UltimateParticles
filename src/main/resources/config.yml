#---------------------------#
#     UltimateParticles     #
#---------------------------#

image-settings:
  min-size: 8 # Has to be greater than 0 and smaller than the maximum size
  max-size: 128 # Errors may occur if the maximum size is greater than 128 because the maximum particle amount is approx. 20000 particles.